<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="Test_android_app.MainPage"
             xmlns:viewmodel="clr-namespace:Test_android_app.ViewModel"
             x:DataType="viewmodel:MainViewModel">
    <!--The above code sets up a MainViewModel class using code generated by MVVM (a NuGet package)-->
    <!--"This generator is not generating files"-->
    
    <!--Is the class name named correctly?  It was testAndroidApp.MainPage-->

    <!--Rows: 100 pixels wide, automatically scale the height, and finally, the * means to fill the rest of the list with blank space -->
    <!--Columns: Scale to 75% of horizontal space and 25% of vertical,dotnet_bot.png-->

    <Grid RowDefinitions="100, Auto, *" 
          ColumnDefinitions=".75*, .25*"
          Padding="20">

        <Image Grid.ColumnSpan="2"
                   Source="football.png"/>

        <Entry Placeholder="Search by date"
               Text="{Binding Text}"
               Grid.Row="1"/>

        <Button Text="Search"
                Command="{Binding AddCommand}"
                Grid.Row="1"
                Grid.Column="1"/>

        <CollectionView Grid.Row="2" Grid.ColumnSpan="2"
                        ItemsSource="{Binding Items}"
                        SelectionMode="None">
            
            <CollectionView.ItemTemplate>
                <DataTemplate x:DataType="{x:Type x:String}">
                    <SwipeView>
                        <SwipeView.RightItems>
                            <SwipeItems>
                                <SwipeItem Text="Delete"
                                           BackgroundColor="Red"
                                           Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:MainViewModel}}, Path=DeleteCommand}"
                                           CommandParameter="{Binding .}"/>
                            </SwipeItems>
                        </SwipeView.RightItems>
                        <Grid Padding="0,5">
                            <Frame>
                                <Frame.GestureRecognizers>
                                    <TapGestureRecognizer
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type viewmodel:MainViewModel}}, Path=TapCommand}"
                                           CommandParameter="{Binding .}"/>
                                </Frame.GestureRecognizers>
                                <Label Text="{Binding .}"
                                   FontSize="24"/>
                            </Frame>
                        </Grid>
                    </SwipeView>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>

    </Grid>
 

</ContentPage>
